{
  "name": "sitelemetry-audit-action",
  "version": "0.2.1",
  "private": true,
  "description": "GitHub Action and GitLab CI/CD component that run a Sitelemetry website audit and report SARIF, a GitLab security report, a summary and one pull or merge request comment.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/run.mjs && node --check src/audit.mjs && node --check src/mcp-client.mjs && node --check src/outcome.mjs && node --check src/sarif.mjs && node --check src/report.mjs && node --check src/plans.mjs && node --check src/github.mjs && node --check src/platform.mjs && node --check src/gitlab.mjs && node --check src/gitlab-report.mjs"
  }
}
